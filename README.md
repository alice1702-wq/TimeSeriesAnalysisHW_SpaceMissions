# TimeSeriesAnalysisHW_SpaceMissions
Финальное задание по предмету Анализ временных рядов, студентки 1 курса магистратуры, Селезневой Алисы

## Актуальность
В качестве данных для финальной работы был выбран датасет All Space Missions from 1957. Он представляет особый интерес в связи с повышенным исследованием космоса в последние годы, а также с желанием проследить эволюцию глобальной космической активности за более чем шестидесятилетний период — от первых спутниковых запусков до современной эры коммерческих и межпланетных миссий.

Актуальность темы обусловлена несколькими факторами:
- Рост интенсивности запусков. В последнее десятилетие число запусков резко выросло благодаря частным компаниям (SpaceX и др.) и мегапроектам (Starlink). Прогнозирование необходимо для планирования ресурсов космодромов и анализа рынка.
- Структурные изменения. Доля коммерческих операторов быстро увеличивается, меняя сезонность и тренды ряда. Прогнозные модели позволяют оценить вклад разных секторов и предсказать дальнейшие сдвиги.
- Ряд обладает нелинейным трендом, мультипликативной сезонностью и выбросами, что делает его полигоном для сравнения статистических, ML и DL методов. Результаты важны для оценки применимости подходов к нестационарным процессам.
- Практическая востребованность. Прогноз на 6 месяцев нужен операторам космодромов, страховщикам, производителям спутников и финансовым аналитикам. Разработанный пайплайн может быть адаптирован для смежных задач (авиатрафик, морские порты).

## 1. Описание ВР и постановка задачи 
Исходный массив содержал 4324 записи о космических запусках с 1957 по 2020 год. Каждая запись включала 8 атрибутов: дату запуска, компанию, локацию, тип ракеты и др.
Было установлено, что столбец Datum хранит дату в строковом формате с двумя вариантами записи (с временем UTC и без времени). Обнаружено 115 пропусков в столбце Rocket (≈2.7% от общего объёма), что не является критичным для последующей агрегации по временным меткам.

## 2. Постановка задачи прогнозирования

- **Цель:** прогноз количества космических запусков на **6 месяцев вперёд** (горизонт `h = 6`).
- **Тип модели:** глобальная.
- **Режим:** офлайн-прогнозирование.
- **Метрики качества:** MAE как основная – она интерпретируема в единицах измерения (количество запусков); RMSE – для штрафования крупных отклонений.
- **Предобработка:** логарифмирование и дифференцирование (первое порядка и сезонное с шагом 12) для приведения к стационарности.

## 3. EDA
Основная информация:
- Ряд охватывает период с октября 1957 года по ноябрь 2020 года включительно.
- Количество наблюдений – 746 (полные месяцы без пропусков).
- В последние месяцы значение count достигает 14, что заметно выше среднего (5.79) и указывает на восходящий тренд в конце периода наблюдения. Абсолютный максимум ряда (18) был зафиксирован в более ранние годы.

В ходе расчёта основных статистик было установлено, что среднее количество запусков в месяц составляет 5.79, а медиана – 5.00. Превышение среднего над медианой указывает на слабую положительную асимметрию распределения. Коэффициент вариации (отношение стандартного отклонения 3.07 к среднему) равен 0.53, что свидетельствует об умеренной волатильности процесса. Минимальное значение равно 1, то есть ни один месяц за весь период не остался без запусков. Максимальное значение достигает 18, а межквартильный размах (от 4 до 8) составляет 4. Таким образом, ряд характеризуется отсутствием нулей, наличием редких пиков и достаточно устойчивой центральной тенденцией.
<img width="1054" height="475" alt="image" src="https://github.com/user-attachments/assets/aecdaa40-ce5d-4867-a819-d380fee37eb1" />
Таким образом, график подтверждает нестационарность ряда, наличие тренда и сезонности, что требует соответствующих методов предобработки при построении прогнозных моделей.

<img width="989" height="807" alt="image" src="https://github.com/user-attachments/assets/9d837d30-4b29-42c8-8aaa-ed6bccdb8796" />

В ходе декомпозиции было установлено, что тренд имеет нелинейную форму. На графике тренда выделяются три характерные фазы: активный рост в 1960-е годы, затем длительный период относительной стагнации и спада в 1970–1990-е годы, и, наконец, устойчивый ускоренный рост после 2000 года. Это свидетельствует о наличии структурных сдвигов в исследуемом процессе.

Сезонная компонента устойчиво повторяется с периодом 12 месяцев. Однако было обнаружено, что амплитуда сезонных колебаний возрастает со временем – от ±1 запуска в ранние годы до ±2.5–3 запусков в последние десятилетия. Данный факт указывает на мультипликативный характер сезонности, тогда как использованная аддитивная модель является лишь приближением.

Остатки центрированы около нуля, что говорит о корректности выделения тренда и сезонности. Вместе с тем, в остаточной компоненте присутствуют выраженные выбросы, соответствующие историческим пикам космической активности (например, 1967, 1975, 2015 годы). Кроме того, наблюдается непостоянство дисперсии остатков – явление условной гетероскедастичности, которое может потребовать преобразования ряда (например, логарифмирования) перед моделированием.

Расширенный тест Дики–Фуллера дал следующие результаты: ADF-статистика = –2.426, p‑value = 0.1345. Критические значения составляют –3.439 (1%), –2.865 (5%), –2.569 (10%). Поскольку p‑value превышает 0.05, нулевая гипотеза о наличии единичного корня не отвергается. Следовательно, ряд является нестационарным, что полностью согласуется с визуальным анализом трендовой компоненты.

<img width="1223" height="792" alt="image" src="https://github.com/user-attachments/assets/4bd0581e-5adb-4244-ba26-767bdf15f150" />

**ACF и PACF**

- ACF медленно затухает, с колебаниями и максимумом на лаге 6 (0.445), что подтверждает нестационарность и наличие сложной сезонной/циклической структуры (возможно, полугодовой цикл).
- PACF резко падает после второго лага (с 0.362 → 0.336 → 0.210) и становится незначимой, что характерно для процесса авторегрессии второго порядка – AR(2).

### На основе выполненного анализа сформулированы следующие заключения:
- Ряд нестационарен (ADF p-value = 0.1345), имеет нелинейный восходящий тренд и мультипликативную годовую сезонность.
- ACF затухает медленно с колебаниями (максимум на лаге 6 – 0.445), PACF резко обрывается после второго лага, что характерно для процесса AR(2) после дифференцирования.
- Присутствуют выбросы (1967, 1975, 2015 гг.) и гетероскедастичность, поэтому перед моделированием целесообразно применить логарифмирование, затем первое дифференцирование и сезонное дифференцирование с шагом 12.
- Сохранённый временной ряд (`monthly_space_missions.csv`) будет использован в задачах №2–4 для бэктестинга и сравнения моделей.

## 4. Выбор методов анализа временного ряда

В работе сравниваются три группы моделей. Параметры моделей обоснованы ниже.

| Категория | Модели | Обоснование выбора параметров |
|-----------|--------|-------------------------------|
| **Статистические (бейзлайн)** | Naive, SeasonalNaive, AutoARIMA, ETS, Theta | Naive – простейший бейзлайн; SeasonalNaive учитывает годовую сезонность; AutoARIMA автоматически подбирает порядки (p,d,q) по AIC; ETS – экспоненциальное сглаживание с автоматическим выбором тренда/сезонности; Theta – метод, хорошо зарекомендовавший себя на сезонных рядах. Для всех моделей сезонный период задан равным 12. Модели Naive и SeasonalNaive использовались в ручном режиме (фиксированные параметры), тогда как AutoARIMA, AutoETS и Theta – в автоматическом (подбор параметров на основе данных). |
| **Машинное обучение** | Random Forest, XGBoost, LightGBM | Использованы лаговые признаки (1,2,3,6,12,24), скользящие статистики (rolling mean 12) и календарные признаки (месяц, квартал). Количество деревьев – 100, остальные параметры по умолчанию. Выбор этих моделей обусловлен их способностью улавливать нелинейные зависимости. |
| **Глубокое обучение** | N-HiTS, TFT, PatchTST | Архитектуры, специализированные для долгосрочного прогнозирования. Параметры: `input_size = 24`, `max_epochs = 50`, ранняя остановка. Выбраны как современные методы, показывающие высокие результаты на задачах с трендом и сезонностью. |

## 5. Сравнение статистических моделей прогнозирования

Оценка качества производилась методом бэктестинга с расширяющимся окном. Параметры валидации:
- Горизонт прогнозирования – 6 месяцев;
- Тестовый период – последние 24 месяца (4 окна по 6 месяцев);
- Шаг между окнами – 6 месяцев;
- Модели не переобучались на каждом шаге (refit=False), что соответствует условиям офлайн-прогнозирования.
В качестве метрик использованы MAE как основная – она интерпретируема в единицах количества запусков, и RMSE – для учёта крупных отклонений.

### Сравнение статистических моделей по MAE и RMSE

| Модель          | MAE   | RMSE  |
|-----------------|-------|-------|
| Naive           | 3.958 | 4.686 |
| SeasonalNaive   | 2.625 | 3.434 |
| AutoARIMA       | 2.529 | 3.071 |
| AutoETS         | 2.694 | 3.338 |
| Theta           | 2.681 | 3.321 |

<img width="1213" height="718" alt="image" src="https://github.com/user-attachments/assets/a11543d7-19d3-4df5-8891-6304bf32b61c" />

Визуальный анализ показывает, что модели AutoARIMA и SeasonalNaive дают наиболее близкие к фактическим значениям прогнозы. Однако обращает на себя внимание систематическое завышение прогнозов моделями Naive, SeasonalNaive, AutoARIMA, AutoETS и Theta в марте–июне 2020 года. Это может быть связано с влиянием внешнего фактора – пандемии COVID-19, которая привела к снижению космической активности в первой половине 2020 года. Статистические модели, обученные на исторических данных, не смогли учесть это структурное изменение.

В августе 2020 года фактическое значение составило 3 запуска, тогда как все модели дали прогнозы в диапазоне 9–12 запусков. Это указывает на ограниченность статистических методов в условиях внешних шоков. Модель Naive демонстрирует наихудший результат, так как не учитывает ни тренд, ни сезонность. SeasonalNaive повторяет сезонную волну прошлого года, но не адаптируется к изменению амплитуды.

Таким образом, график подтверждает, что статистические модели не способны улавливать резкие изменения, вызванные внешними событиями, что обосновывает необходимость использования более гибких методов машинного и глубокого обучения.

Для проверки адекватности лучшей статистической модели (AutoARIMA) был проведён анализ остатков. Модель обучена на всём ряде, после чего получены остатки.

<img width="1206" height="783" alt="image" src="https://github.com/user-attachments/assets/ae4aadd2-7ddc-46b1-bdf7-f9a8509e39fb" />

Анализ результатов привел к следующим выводам: 
- Среднее значение остатков составило 0.0643, что близко к нулю. Это свидетельствует об отсутствии систематического смещения и подтверждает корректную спецификацию модели – она не завышает и не занижает прогнозы в среднем.
- Тест Льюнга-Бокса дал следующие значения: p‑value = 0.7878 для 12 лагов и p‑value = 0.9536 для 24 лагов. Оба значения значительно превышают порог 0.05, следовательно, нулевая гипотеза об отсутствии автокорреляции не отвергается. Остатки модели являются белым шумом – это означает, что модель правильно идентифицировала и учла всю доступную временную структуру ряда (тренд, сезонность и авторегрессионную компоненту).
- Гистограмма и Q-Q plot указывают на распределение, близкое к нормальному. Небольшие отклонения в хвостах связаны с наличием исторических выбросов (1967, 1975, 2015 годы), которые модель не может полностью сгладить из-за их единичного характера.
- Стандартное отклонение остатков составляет 2.3961, что меньше стандартного отклонения исходного ряда (3.07). Это свидетельствует о том, что модель объясняет значительную долю вариации процесса.
- Несмотря на адекватность, модель не способна предсказывать резкие аномалии, связанные с внешними событиями (например, снижение активности в 2020 году из-за пандемии COVID-19). Это связано с предположением ARIMA о постоянстве дисперсии, что открывает возможности для улучшения прогнозов с помощью нелинейных методов.

<img width="1024" height="686" alt="image" src="https://github.com/user-attachments/assets/0a7c6f9f-7a21-4630-bc94-066bff741a9f" />

Прогноз модели AutoARIMA на 6 месяцев (сентябрь 2020 – февраль 2021) варьируется от 8.38 до 10.65 запусков в месяц, с пиком в декабре. Средняя полуширина 95% доверительного интервала составляет ±4.74 запуска, что указывает на высокую неопределённость – интервал перекрывает почти всю область возможных значений. Фактические данные августа 2020 (3 запуска) выходят за нижнюю границу прогноза на сентябрь (4.39), что подтверждает неспособность модели учитывать внешние шоки. Широкие интервалы обосновывают переход к более гибким методам (ML/DL) для повышения надёжности прогнозов.

### На основе проведённого бэктестинга пяти статистических моделей были получены следующие результаты:
- Лучшая модель: AutoARIMA показала наилучшие результаты по обеим метрикам: MAE = 2.529, RMSE = 3.071. Она превосходит простейший бенчмарк Naive на 36.1% по MAE, что подтверждает наличие значимой авторегрессионной и сезонной структуры в ряду, выявленной на этапе EDA.
- Сезонность играет важнейшую роль: Модель SeasonalNaive (MAE = 2.625) оказалась всего на 0.096 хуже, чем AutoARIMA. Это говорит о том, что годовая сезонность является доминирующим фактором динамики запусков, и простое повторение значений за аналогичный месяц прошлого года даёт почти такую же точность, как и сложный автоматический подбор ARIMA.
- Все модели продемонстрировали систематическую ошибку в периоды внешних шоков (например, спад 2020 года из-за пандемии COVID-19) и неспособность предсказывать резкие пиковые значения. Это связано с линейной природой методов и предположением о постоянстве дисперсии.
- Анализ ошибок по отдельным окнам показал, что AutoARIMA имеет наименьшую вариативность MAE, что подтверждает её надёжность.
- 95% доверительный интервал для AutoARIMA имеет среднюю полуширину ±4.74 запуска, что указывает на высокую неопределённость на горизонте 6 месяцев.

**Вывод:** Модель AutoARIMA признана наилучшей статистической моделью и будет использована в качестве бенчмарка для сравнения с методами машинного и глубокого обучения в Задаче №3. Ожидается, что нелинейные подходы позволят улучшить точность прогнозирования в периоды аномалий и дадут более узкие доверительные интервалы. Полученные результаты (MAE = 2.529 для AutoARIMA) будут использованы как бенчмарк для сравнения с ML и DL моделями.

# 6. Сравнение ML-моделей 

Для прогнозирования количества космических запусков были выбраны три модели машинного обучения, реализованные в библиотеке mlforecast: Random Forest, XGBoost, LightGBM.
Для всех моделей были созданы следующие признаки:
- Лаговые значения: 1, 2, 3, 6, 12, 24 месяца (учёт краткосрочной, среднесрочной и годовой памяти ряда).
- Скользящие статистики: средние и стандартные отклонения для окон 3, 6, 12 месяцев.
- Расширяющиеся средние для лагов 12 и 24 (учёт долгосрочного тренда).
- Календарные признаки: месяц, квартал, день недели (учёт сезонности).
- Дифференцирование с шагом 12 для приведения ряда к стационарности.
Параметры моделей: количество деревьев – 100, остальные параметры оставлены по умолчанию. Выбор этих моделей обусловлен их способностью улавливать нелинейные зависимости и взаимодействия между признаками.

### Сравнение ML моделей по MAE и RMSE

| Модель          | MAE   | RMSE  |
|-----------------|-------|-------|
| Random Forest   | 2.773 | 3.490 |
| XGBoost         | 2.794 | 3.782 |
| LightGBM        | 2.558 | 3.382 |

<img width="1429" height="685" alt="image" src="https://github.com/user-attachments/assets/ab6d9c7f-8712-42a5-af58-65705cf508b8" />

Визуальный анализ показывает, что LightGBM и Random Forest дают наиболее близкие к фактическим значениям прогнозы. Все ML модели демонстрируют систематическое завышение прогнозов в марте–июне 2020 года (прогнозы 5–8 запусков против фактического 1). Это опять же связано с влиянием пандемии COVID-19, как и в случае со статистическими моделями. В августе 2020 года (факт 3 запуска) прогнозы всех моделей составили 5–8 запусков, что указывает на ограниченность ML методов в условиях внешних шоков без экзогенных переменных. LightGBM демонстрирует наиболее сглаженный прогноз, что согласуется с его лучшими метриками (MAE = 2.558).

Для проверки адекватности лучшей ML-модели (LightGBM) был проведён анализ остатков. Модель обучена на всём ряде с использованием тех же признаков, что и при бэктестинге.
<img width="1191" height="782" alt="image" src="https://github.com/user-attachments/assets/51e631c6-ad41-4028-9071-ca2cd7f877cd" />

Выводы:
- Среднее остатков близко к нулю (0.012), что свидетельствует об отсутствии систематического смещения.
- Стандартное отклонение остатков составляет 2.34, что меньше, чем у исходного ряда (3.07), — модель объясняет значительную долю вариации.
- Тест Льюнга-Бокса дал p‑value = 0.32 для 12 лагов и 0.56 для 24 лагов. Оба значения превышают 0.05, следовательно, гипотеза об отсутствии автокорреляции не отвергается — остатки являются белым шумом.
- Гистограмма и Q-Q plot указывают на распределение, близкое к нормальному, с небольшими отклонениями в хвостах (связаны с историческими выбросами).
Таким образом, остатки модели LightGBM соответствуют белому шуму, что подтверждает её статистическую адекватность.

<img width="1409" height="712" alt="image" src="https://github.com/user-attachments/assets/81433587-b61a-42eb-b8e9-b98815057a81" />

Выводы:
- Средняя полуширина 95% доверительного интервала составила ±6.63 запуска, что шире, чем у AutoARIMA (±4.74). Это указывает на более высокую неопределённость прогнозов ML-модели по сравнению со статистической.
- Фактические значения 2020 года выходят за границы 95% интервала, что подтверждает неспособность модели предсказывать внешние шоки без экзогенных переменных.
- Прогноз LightGBM является практически постоянным (7.68–8.87), что указывает на то, что модель не улавливает сезонные колебания, характерные для ряда (пики в декабре). Это может быть связано с ограниченным объёмом выборки или недостаточностью признаков для моделирования сезонности.

### Выводы по ML-моделям:
- Лучшая ML-модель – LightGBM (MAE = 2.558, RMSE = 3.382). Она практически не уступает лучшей статистической модели AutoARIMA (MAE = 2.529) — разница составляет всего 0.029 (1.1%).
- Все ML-модели демонстрируют систематическую ошибку в периоды внешних шоков (пандемия 2020), так как в обучающей выборке отсутствовали аналогичные события. Это ограничение может быть преодолено добавлением экзогенных переменных (экономические индикаторы, данные о пандемии).
- Для оперативных прогнозов LightGBM может быть рекомендован как альтернатива AutoARIMA при наличии вычислительных ресурсов и возможности добавлять дополнительные признаки. Однако приоритет следует отдавать AutoARIMA из-за её большей надёжности и более узких доверительных интервалов.

# 7. Сравнение DL- моделей

Для прогнозирования количества космических запусков были выбраны три современные архитектуры глубокого обучения, реализованные в библиотеке neuralforecast: NHITS, TFT, PatchTST.
Параметры обучения:
- input_size = 24 (2 года истории для прогноза на 6 месяцев) 
- max_epochs = 50 
- early_stop_patience_steps = 5
- random_seed = 42 
Для улучшения качества модели TFT была проведена дополнительная настройка гиперпараметров:
- input_size = 36 
- max_steps = 150 
- early_stop_patience_steps = 20
Все модели обучались с валидационной выборкой (val_size = 12 месяцев). Бэктестинг проводился методом расширяющегося окна с теми же параметрами, что и для статистических и ML-моделей (горизонт 6 месяцев, 4 окна, шаг 6 месяцев).

| Модель    | MAE   | RMSE  |
|-----------|-------|-------|
| NHITS     | 2.740 | 3.354 |
| TFT       | 2.660 | 3.382 |
| PatchTST  | 2.730 | 3.328 |

Лучшей базовой DL моделью оказался TFT с MAE = 2.660 и RMSE = 3.382. Он превосходит NHITS и PatchTST по MAE, однако все DL модели уступают лучшей статистической модели AutoARIMA (MAE = 2.529) и лучшей ML модели LightGBM (MAE = 2.558). Это указывает на то, что при ограниченном объёме выборки (747 точек) сложные нейросетевые архитектуры не всегда превосходят более простые методы.

<img width="1459" height="698" alt="image" src="https://github.com/user-attachments/assets/0ab80f64-a81a-4dff-aedd-b34b73620678" />

Визуально все DL модели дают сглаженные прогнозы, но заметно завышают значения в марте–июне 2020 года. TFT и PatchTST дают наиболее близкие к фактическим значениям прогнозы в августе 2020. Однако все модели систематически завышают прогнозы в период пандемии, что указывает на их неспособность учитывать внешние шоки без экзогенных переменных.

Для проверки адекватности улучшенной модели TFT был проведён анализ остатков. Модель обучена на всём ряде, после чего получены остатки.
<img width="1298" height="783" alt="image" src="https://github.com/user-attachments/assets/2ab3f097-95cd-4ac3-9ab4-8ca8b4c16a65" />

Выводы:
- Среднее остатков близко к нулю (≈ -0.01), что свидетельствует об отсутствии систематического смещения.
- Стандартное отклонение остатков составляет 2.18, что меньше, чем у исходного ряда (3.07) и близко к показателям лучших моделей.
- Тест Льюнга-Бокса дал p‑value = 0.44 для 12 лагов и 0.68 для 24 лагов. Оба значения превышают 0.05, следовательно, гипотеза об отсутствии автокорреляции не отвергается — остатки являются белым шумом.
- Гистограмма и Q-Q plot указывают на распределение, близкое к нормальному, с небольшими отклонениями в хвостах (связаны с историческими выбросами).
Таким образом, остатки модели TFT соответствуют белому шуму, что подтверждает её статистическую адекватность. Модель корректно идентифицировала основную структуру ряда, но не способна предсказывать резкие аномалии.

Для модели TFT построены приближённые доверительные интервалы 80% и 95% на основе RMSE = 3.382, полученного при бэктестинге. Интервалы рассчитаны с использованием нормального приближения: 80% интервал — прогноз ±1.28·RMSE, 95% интервал — прогноз ±1.96·RMSE.
<img width="1463" height="685" alt="image" src="https://github.com/user-attachments/assets/fbb468a7-3034-42b9-950b-ede0361aa5b3" />

Выводы: 
- Средняя полуширина 95% доверительного интервала составила ±6.63 запуска, что значительно шире, чем у AutoARIMA (±4.74). Это указывает на высокую неопределённость прогнозов TFT на данном ряде.
- Фактические значения 2020 года выходят за границы 95% интервала, что подтверждает неспособность модели предсказывать внешние шоки без экзогенных переменных.
- Широкие интервалы объясняются высокой остаточной дисперсией, которую TFT не может полностью объяснить при малом объёме выборки.
Таким образом, несмотря на улучшение параметров, вероятностный прогноз TFT остаётся менее точным и более неопределённым, чем у AutoARIMA. Это говорит о том, что для данного ряда классические статистические методы обеспечивают лучший баланс между точностью и надёжностью прогнозов.

### Выводы по DL-моделям:
- Лучшая базовая DL модель – TFT (MAE = 2.660, RMSE = 3.382). После улучшения гиперпараметров (input_size=36, max_steps=150) её точечный прогноз не показал значительного улучшения по сравнению с бэктестингом.
- Все DL модели уступают AutoARIMA (MAE = 2.529) и LightGBM (MAE = 2.558). Это связано с малым объёмом выборки (747 точек) и отсутствием экзогенных переменных, что ограничивает способность нейросетей обучаться сложным закономерностям.
- Для оперативного прогнозирования на данном ряде предпочтительнее использовать AutoARIMA. DL-модели могут быть применены при наличии большего объёма данных или дополнительных внешних признаков, способных объяснить аномалии (экономические индикаторы, данные о пандемии).

# 8. Подготовка пайплайна 
Разработанный пайплайн представляет собой универсальный инструмент, предназначенный для решения задач прогнозирования временных рядов. Его ключевая особенность — модульность и гибкость: архитектура класса ForecastingPipeline позволяет использовать любую модель регрессии, совместимую с интерфейсом scikit-learn, что даёт возможность подбирать оптимальный алгоритм под конкретную задачу без изменения структуры пайплайна. В качестве демонстрационной модели для пайплайна был выбран градиентный бустинг LightGBM, и этот выбор обусловлен комплексом причин, вытекающих из результатов проведённого исследования.

<img width="1052" height="583" alt="image" src="https://github.com/user-attachments/assets/d837cca3-6f8f-417f-9c1d-b7f716efcb7c" />

Прежде всего, LightGBM показал второй по точности результат среди всех протестированных моделей, уступив лишь статистической модели AutoARIMA. Согласно результатам бэктестинга, его средняя абсолютная ошибка составила MAE = 2.558, тогда как AutoARIMA показала MAE = 2.529 — разница составляет всего 0.029, что свидетельствует о том, что LightGBM практически догнал абсолютного лидера по точности. При этом LightGBM значительно превзошёл другие ML-модели, включая Random Forest (MAE = 2.773) и XGBoost (MAE = 2.794). Таким образом, LightGBM представляет собой оптимальный баланс между точностью и вычислительной эффективностью, обеспечивая качество, близкое к наилучшему, но с существенно большей гибкостью. Кроме того, LightGBM отличается высокой скоростью обучения и прогнозирования: как показало тестирование производительности, обучение модели на всём историческом ряде занимает менее 0.5 секунды, а рекурсивный прогноз на 6 месяцев вперёд — менее 0.2 секунды. Это делает пайплайн пригодным для использования в режиме реального времени, где требуется быстрое переобучение на новых данных.

Важным преимуществом LightGBM является его способность эффективно работать с большим количеством разнородных признаков. В рамках пайплайна создаются десятки признаков: лаговые значения с различными сдвигами, скользящие статистики, календарные переменные. LightGBM, как и другие алгоритмы градиентного бустинга, автоматически ранжирует важность признаков и строит нелинейные комбинации между ними, что позволяет моделировать сложные зависимости, недоступные для линейных методов. В отличие от AutoARIMA, которая ограничена линейной авторегрессионной структурой, LightGBM способен улавливать нелинейные взаимодействия между сезонностью, трендом и краткосрочными колебаниями, что делает его более универсальным инструментом при расширении набора признаков. Наряду с этим, LightGBM хорошо масштабируется на большие объёмы данных и поддерживает параллельные вычисления, что делает его предпочтительным выбором для задач, где объём данных может расти со временем. В отличие от статистических методов, которые требуют ручного анализа структуры ряда для определения порядка авторегрессии и сезонных компонент, LightGBM автоматически адаптируется к структуре данных через механизм градиентного бустинга, что снижает требования к экспертизе и ускоряет процесс разработки.

Важно подчеркнуть, что выбор LightGBM для демонстрации работы пайплайна не означает, что эта модель является единственно возможной или абсолютно лучшей для всех сценариев. Архитектура пайплайна оставляет возможность замены модели в любой момент — пользователь может инициализировать пайплайн с AutoARIMA, TFT, Random Forest или любым другим регрессором, просто передав другой объект в конструктор. LightGBM выбран именно как демонстрационный пример, поскольку он сочетает в себе высокую точность, скорость и гибкость, что позволяет наиболее полно показать возможности пайплайна без привязки к конкретному узкому классу задач. При этом рекомендация для оперативного прогнозирования остаётся за AutoARIMA как за наиболее точной моделью, а LightGBM предлагается как альтернатива в тех случаях, когда требуется быстрая адаптация к новым данным или возможность интеграции внешних признаков. Таким образом, пайплайн с LightGBM иллюстрирует практическую реализацию подхода, который при необходимости может быть перенастроен под любую модель, обеспечивая баланс между точностью, скоростью и гибкостью.

| Метод | MAE | RMSE | Улучшение MAE | Улучшение RMSE |
|-------|-----|------|---------------|----------------|
| Naive (последнее значение) | 4.156 | 5.468 | — | — |
| Пайплайн (LightGBM, рекурсивный) | 3.600 | 4.136 | 13.5% | 24.4% |

Пайплайн превосходит наивный прогноз на 13.5% по MAE и на 24.4% по RMSE. Это подтверждает, что использование лаговых и календарных признаков даёт значительное улучшение качества прогноза по сравнению с простейшим бейзлайном. Статистическая значимость улучшения подтверждается устойчивостью результатов на кросс-валидации (стандартное отклонение MAE для LightGBM составляет 1.779, что близко к показателям лучших моделей).

# 9. Отчет об исследовании ВР
В ходе выполнения работы был проведён полный цикл анализа временного ряда «Космические запуски (1957–2020)», содержащего 747 ежемесячных наблюдений. Целью исследования являлось прогнозирование количества запусков на горизонт 6 месяцев. Для решения поставленной задачи были выполнены разведочный анализ, бэктестинг пяти статистических, трёх моделей машинного обучения и трёх моделей глубокого обучения, а также разработан универсальный пайплайн для автоматизации прогнозирования.

| Категория | Модель | MAE | RMSE |
|-----------|--------|-----|------|
| Статистические | AutoARIMA | **2.529** | **3.071** |
| | SeasonalNaive | 2.625 | 3.434 |
| | AutoETS | 2.694 | 3.338 |
| | Theta | 2.681 | 3.321 |
| | Naive | 3.958 | 4.686 |
| Машинное обучение | LightGBM | 2.558 | 3.382 |
| | Random Forest | 2.773 | 3.490 |
| | XGBoost | 2.794 | 3.782 |
| Глубокое обучение | TFT | 2.660 | 3.382 |
| | PatchTST | 2.730 | 3.328 |
| | NHITS | 2.740 | 3.354 |
| **Пайплайн** | **LightGBM** | **3.600** | **4.136** |

**Ключевые результаты исследования**
1. Модель AutoARIMA продемонстрировала наилучшие результаты по обеим метрикам: MAE = 2.529, RMSE = 3.071. Она превосходит наивный прогноз Naive (MAE = 3.958) на 36.1%, что подтверждает наличие значимой авторегрессионной и сезонной структуры в ряду, выявленной на этапе EDA (PACF с пиком на лаге 1, ACF с сезонными колебаниями на лагах 12 и 24). Анализ остатков подтвердил адекватность модели: остатки являются белым шумом (тест Льюнга-Бокса p > 0.05), распределение близко к нормальному. Доверительные интервалы AutoARIMA (95% ДИ: ±4.74) являются наиболее узкими среди всех моделей, что делает её предпочтительным выбором для принятия решений в условиях неопределённости.

2. Модель SeasonalNaive (MAE = 2.625) оказалась всего на 0.096 хуже, чем AutoARIMA, и заняла второе место по точности. Это является важнейшим выводом исследования: годовая сезонность является ключевой детерминантой динамики космических запусков. Простое повторение значений за аналогичный месяц прошлого года даёт почти такую же точность, как и сложный автоматический подбор ARIMA. Этот результат объясняет, почему сложные ML и DL модели не смогли существенно превзойти статистические методы — они пытаются моделировать нелинейности там, где доминирует простая линейная сезонная компонента.

3. Модели машинного обучения – близки к статистическим

LightGBM (MAE = 2.558) практически догнал AutoARIMA (разница всего 0.029). Это показывает, что при правильном подборе признаков (лаги, скользящие средние, календарные переменные) градиентный бустинг способен конкурировать с классической ARIMA. Однако анализ остатков и доверительных интервалов (95% ДИ: ±6.63) выявил, что LightGBM даёт более широкие интервалы, что указывает на меньшую уверенность модели в своих предсказаниях. Random Forest (MAE = 2.773) и XGBoost (MAE = 2.794) показали более скромные результаты, что объясняется меньшей способностью моделировать сложные взаимодействия признаков.

4. Лучшая DL модель — TFT (MAE = 2.660) — уступает AutoARIMA на 4.9% и LightGBM на 3.8%. Улучшение гиперпараметров (input_size=36, max_steps=150) не дало значительного прироста точности. Анализ остатков TFT подтвердил адекватность модели (белый шум, p > 0.05), однако доверительные интервалы (95% ДИ: ±6.63) оказались такими же широкими, как у LightGBM. Это объясняется малым объёмом выборки (747 точек), недостаточным для эффективного обучения нейросетей, особенно архитектур с механизмами внимания. NHITS (MAE = 2.740) и PatchTST (MAE = 2.730) показали сравнимые результаты, но также уступили статистическим методам.

5. Наибольшие ошибки всех моделей пришлись на март–июнь 2020 года (пандемия COVID-19), когда фактическое число запусков резко снизилось до 1–3 в месяц. Ни одна из моделей не смогла предсказать это структурное изменение, так как в обучающей выборке отсутствовали аналогичные события. Это фундаментальное ограничение всех рассмотренных подходов, указывающее на необходимость включения экзогенных переменных (экономические индикаторы, данные о пандемии) для улучшения прогнозов в условиях кризисов.

6. Разработанный пайплайн на базе LightGBM с рекурсивным прогнозированием показал MAE = 3.600 и RMSE = 4.136 на тестовом периоде, что на 13.5% лучше наивного прогноза. Время обучения составляет менее 0.5 секунды, прогноза — менее 0.2 секунды, что делает его пригодным для промышленного использования. Гибкая архитектура позволяет легко заменять модель и добавлять новые признаки, что открывает возможности для дальнейшего улучшения.

Данная работа демонстрирует применимость различных классов моделей к реальным временным рядам с трендом, сезонностью и структурными сдвигами. Классические статистические методы (AutoARIMA) остаются наиболее эффективными при ограниченном объёме данных, однако методы машинного обучения (LightGBM) показывают сопоставимые результаты и открывают возможности для дальнейшего улучшения при добавлении дополнительной информации. Глубокое обучение на данном ряде не дало преимущества, что объясняется малым объёмом выборки. Разработанный пайплайн предоставляет практический инструмент, готовый к адаптации для широкого круга задач прогнозирования.

